module Tdip.Service.Screening.Screen
typealias Pages = Mapping<String, String>

typealias Identifier = String(matches(Regex(#"(^[a-zA-Z_$][\w$]*$)"#)))

class ScreenProps {
  factors: List<Identifier>;
  universes: List<Identifier>
}

id: Identifier
fixed type: String = getClass().toString()
props: ScreenProps
index: String = "/"
pages: Pages
